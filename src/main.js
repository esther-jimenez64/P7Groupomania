import { createApp } from 'vue'
import App from './views/App.vue'
import router from './router/index.js'
createApp(App).use(router).mount('#app')
/*Montage de l'application sur argument "container" id, qui est un élément DOM*/
/* défini Le composant racine*/ 
/*créer une nouvelle instance d'application avec la createAppfonction */

